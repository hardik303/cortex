# Rust build artifacts
agent/target/

# Local config (contains DB credentials)
config.toml

# Superset local state
superset-venv/
superset.db
superset_home/

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Video generation artifacts (frame directories + root-level scratch mp4s)
demo_frames/
pg_frames/
/cortex_demo.mp4
/cortex_postgres_demo.mp4
